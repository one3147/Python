원래 10진법으로 나타내진 수를 2, 8, 16진수로 변환하기 위해선 알고리즘을 설계해야한다.
그러나, 파이썬은 알고리즘을 설계하기 어려워하는 초보자들을 위해 진수간의 변환을 자동으로 해주는 기능을 만들어놓았다.
심지어 라이브러리를 따로 추가하지 않아도 된다.

2진법 = bin 8진법 = oct 10진법 = dec 16진법 = hex
ex)
a = 10
b = hex(a)
이런 식으로 쓰면, b는 10을 16진수로 변환한 값을 가지게 된다.
